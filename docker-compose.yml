services:
  document-collection-backend:
    build:
      context: ..
      dockerfile: ./Dockerfile
    env_file:
      - ./.env
    volumes:
      - ../:/app
    ports:
      - "5005:5005"
    depends_on:
      - mongo